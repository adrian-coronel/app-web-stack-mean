module.exports = {
  /**
   * "secret" es una propiedad exportada del módulo. 
   * Contiene una cadena de texto que se utiliza como clave secreta 
   * para la generación y verificación de tokens de autenticación(firmar tokens). 
   */
  secret: 'mysecrettext'
}